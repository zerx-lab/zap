<env>
  Model: {{ model_id | default("unknown") }}
  Working directory: {{ cwd | default("(unknown)") }}
{%- if shell %}
  Shell: {{ shell.name }}{% if shell.version %} {{ shell.version }}{% endif %}
{%- endif %}
{%- if os %}
  Platform: {{ os.platform }}{% if os.distribution %} ({{ os.distribution }}){% endif %}
{%- endif %}
{%- if git %}
  Is directory a git repo: yes
  Git branch: {{ git.branch | default("(detached)") }}
{%- else %}
  Is directory a git repo: no
{%- endif %}
  Today's date: {{ current_time | default("unknown") }}
</env>